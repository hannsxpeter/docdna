name: CI

# Implements: P-MUST-05

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.8", "3.x"]

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "${{ matrix.python }}"

      - name: Compile registry-declared helpers
        run: |
          python - <<'PY'
          import os
          import py_compile
          import sys

          sys.path.insert(0, "skill/scripts")
          from docdna_runtime import load_registry

          runtime_registry = load_registry("skill")
          for member in runtime_registry["runtime_members"]:
              py_compile.compile(os.path.join("skill", member["path"]), doraise=True)
          PY

      - name: Validate catalog JSON
        run: |
          python -m json.tool skill/catalog/runtimes.json > /dev/null
          python -m json.tool skill/catalog/proofs.json > /dev/null
          for f in skill/catalog/*.json; do
            python -c "import json,sys; json.load(open(sys.argv[1]))" "$f"
          done

      - name: Run tests
        run: python -m unittest discover -s tests

      - name: Source doctor and proof pipeline
        run: |
          python skill/scripts/docdna_doctor.py --json --source-checkout > /tmp/docdna-doctor.json
          python skill/scripts/docdna_proof.py --json > /tmp/docdna-proof-source.json

      - name: Proof output matches stable cross-version fixtures
        run: |
          python skill/scripts/docdna_proof.py --json > /tmp/docdna-proof.json
          python skill/scripts/docdna_proof.py > /tmp/docdna-proof.txt
          diff -u proof/replay/expected-proof-output.json /tmp/docdna-proof.json
          diff -u proof/replay/expected-proof-output.txt /tmp/docdna-proof.txt

      # These files are generated and committed. Snapshot the bytes from the checkout before any
      # helper can rewrite them, then rebuild from a full-history checkout and compare. The generated
      # text deliberately excludes the current commit and wall-clock date, because a tracked file
      # cannot reproducibly contain the identity of the commit that contains it.
      - name: Committed generated artifacts match the code
        run: |
          cp DOCDNA.md /tmp/docdna-committed.md
          cp llms.txt /tmp/docdna-llms-committed.txt
          for _ in 1 2 3; do
            python skill/scripts/docdna_select.py . > /dev/null
          done
          cp DOCDNA.md /tmp/docdna-settled.md
          python skill/scripts/docdna_select.py . > /dev/null
          diff -u /tmp/docdna-settled.md DOCDNA.md
          python skill/scripts/docdna_llms.py . > /dev/null
          diff -u /tmp/docdna-committed.md DOCDNA.md
          diff -u /tmp/docdna-llms-committed.txt llms.txt

      # Every script, end to end, on this repository. The unit tests exercise the functions; this
      # step is the only thing that catches a script that cannot run at all, which is how
      # docdna_llms.py once shipped exiting 2 on any repository without a prior manifest.
      - name: Run the whole pipeline on this repository
        run: |
          python skill/scripts/docdna_scan.py --json . > /dev/null
          python skill/scripts/docdna_select.py . > /dev/null
          python skill/scripts/docdna_check.py . > /dev/null
          python skill/scripts/docdna_status.py --json . > /dev/null
          python skill/scripts/docdna_backfill.py --json . > /dev/null
          python skill/scripts/docdna_llms.py . > /dev/null

      # The dogfood gate. The step above proves every script runs; this one proves the number rule
      # is honest about the one repository whose documentation we can actually adjudicate. A single
      # generated-number or provenance-number finding here is either a real fabrication in docdna's
      # own docs, which has to be fixed in the doc, or a false accusation against correct prose,
      # which has to be fixed in the checker. Neither is allowed to ship. This gate is what stops
      # the number rule being tightened once more into a checker that cries wolf, because docdna's
      # own documentation is the loudest wolf it has.
      - name: docdna survives its own number rule
        run: |
          python skill/scripts/docdna_check.py --json . > /tmp/docdna-dogfood.json
          python - <<'PY'
          import json
          import sys

          REFUSED = ("generated-number", "provenance-number")

          with open("/tmp/docdna-dogfood.json", encoding="utf-8") as handle:
              report = json.load(handle)
          rows = [row for row in report["findings"] if row["kind"] in REFUSED]
          for row in rows:
              print("%s:%s %s %s" % (row.get("path"), row.get("line"), row["kind"], row["detail"]))
          if rows:
              sys.exit("the dogfood gate found %d number findings against docdna itself" % len(rows))
          print("dogfood gate clean: %d documents linted for numbers, 0 flagged"
                % report["numbers"]["linted"])
          PY

      # Every sample of tool output in README.md and skill/SKILL.md, regenerated from the command
      # its marker names and compared byte for byte. It runs in the suite too; it gets its own step
      # because seven review rounds in a row shipped a hand-edited sample, the last one a real
      # capture with two of its eight rows deleted, and a fabricated sample has to fail the build
      # rather than wait for the eighth person to notice.
      - name: Documented samples are real stdout
        run: python -m unittest discover -s tests -p test_readme_samples.py -v

      # The regime files assert current legal fact and carry a verified: date. An unmaintained dated
      # file is worse than none, because it asserts with confidence while being stale. This reports
      # age and fails only past a year, since a skill that puts itself in permanent red on day 181
      # has taught its user to ignore its own output.
      - name: Regulatory references carry a date and are not a year stale
        run: |
          python - <<'PY'
          import datetime, glob, re, sys

          TODAY = datetime.date.today()
          stale, undated = [], []
          for path in sorted(glob.glob("skill/references/regime-facts/*.md")):
              head = open(path, encoding="utf-8").readline()
              match = re.match(r"verified:\s*(\d{4})-(\d{2})-(\d{2})", head.strip())
              if match is None:
                  undated.append(path)
                  continue
              age = (TODAY - datetime.date(*[int(g) for g in match.groups()])).days
              print("%-46s %4d days" % (path, age))
              if age > 365:
                  stale.append("%s is %d days old" % (path, age))
          if undated:
              sys.exit("no verified: date on %s" % ", ".join(undated))
          if stale:
              sys.exit("re-verify against primary sources: %s" % "; ".join(stale))
          PY

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Lint installer
        run: shellcheck install.sh

      - name: Install the skill and run it from the installed location
        run: |
          dest=$(mktemp -d)
          consumer_root=$(mktemp -d)
          CLAUDE_SKILLS_DIR="$dest" ./install.sh claude
          for member in SKILL.md scripts catalog references templates; do
            test -e "$dest/docdna/$member"
          done
          repo="$consumer_root/repo"
          mkdir "$repo"
          cp -R tests/fixtures/internal_service/. "$repo/"
          cd "$consumer_root"
          python "$dest/docdna/scripts/docdna_doctor.py" --json > doctor.json
          python "$dest/docdna/scripts/docdna_proof.py" --json > proof.json
          python "$dest/docdna/scripts/docdna_scan.py" --json "$repo" > scan.json
          python "$dest/docdna/scripts/docdna_select.py" "$repo" > /dev/null
          before=$(find "$repo" -type f -print0 | sort -z | xargs -0 sha256sum | sha256sum)
          python "$dest/docdna/scripts/docdna_status.py" --json "$repo" > status.json
          after_status=$(find "$repo" -type f -print0 | sort -z | xargs -0 sha256sum | sha256sum)
          test "$before" = "$after_status"
          python "$dest/docdna/scripts/docdna_backfill.py" --only build.api-reference --json "$repo" > packet.json
          test ! -e "$repo/docs/build/api-reference.md"
          python - "$repo" <<'PY'
          import json
          import os
          import sys

          doctor = json.load(open("doctor.json", encoding="utf-8"))
          proof = json.load(open("proof.json", encoding="utf-8"))
          status = json.load(open("status.json", encoding="utf-8"))
          packet = json.load(open("packet.json", encoding="utf-8"))
          manifest = json.load(open(os.path.join(sys.argv[1], ".docdna", "manifest.json"),
                                    encoding="utf-8"))
          assert doctor["verdict"] == "pass" and doctor["read_only"] is True
          assert proof["validation"]["mode"] == "installed-registry"
          assert status["read_only"] is True
          assert packet["plans"][0]["fresh_context_packet"]["execution"]["status"] == "ready-for-handoff"
          packet_row = next(row for row in manifest["documents"]
                            if row["id"] == "build.api-reference")
          assert packet_row["write_status"] == "in-progress"
          assert packet_row["plan_generated_at"]
          PY
          python "$dest/docdna/scripts/docdna_check.py" "$repo" > /dev/null
          python "$dest/docdna/scripts/docdna_llms.py" "$repo" > /dev/null
