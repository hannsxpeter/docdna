# Release note style

One line per change, written for someone who does not know the codebase.

- Lead with the user-visible effect, not the module that changed.
- Name the flag or setting a reader has to touch, in backticks.
- Say "no action required" when there is none. Silence reads as an omission.
