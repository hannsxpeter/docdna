# @monorepo/web

The browser client. These commands are declared in this package's own `package.json` and are run
from `apps/web`, not from the repository root.

```sh
bun run dev
```

Type check the component tree:

```sh
bun run check
```
