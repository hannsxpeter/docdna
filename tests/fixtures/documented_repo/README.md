# orderbook

An order and refund service. Routes live in `src/api/routes.py`, configuration in
`src/config/settings.py`, and the schema in `schema/orders.sql`.

The documentation set for this repository is described in `docs/`.
